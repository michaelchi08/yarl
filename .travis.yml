dist: trusty
sudo: required

language:
    - c++

install:
    - travis_wait sudo bash scripts/install/install_deps.bash

script:
    - make && make run_tests

notifications:
    recipients:
        - chutsu@gmail.com
    email:
        on_success: change
        on_failure: always
